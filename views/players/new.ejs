<!DOCTYPE html>
<html>

<head>
	<title>
		<%= title %>
	</title>
	<link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %>.</p>
    <% if (errors) { %>
        <% include ../partials/errors %>
    <% } %> 
    <div>
        <form method='POST' action='/players'>
            <div>
                <span>Name: </span>
                <input type="text" name="name" value="<%= player.name %>">
            </div>
            <div>
                <span>Tag: </span>
                <input type="text" name="tag" value="<%= player.tag %>">
            </div>
            <div>
                <span>Sponsor: </span>
                <input type="text" name="sponsor" value="<%= player.sponsor %>">
            </div>
            <div>
                <span>Rank: </span>
                <input type="text" name="rank" value="<%= player.rank %>">
            </div>
            <div>
                <span>Mains: </span>
                <input type="text" name="unformattedMains" value="<%= player.unformattedMains %>">
            </div>
            <input type="submit" value="Create Player">
        </form>
    </div>
</body>

</html>